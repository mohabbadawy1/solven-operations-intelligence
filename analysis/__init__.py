"""Analysis package for the Solven Operations Intelligence Platform.

Each module in this package is responsible for deriving operational
insights from a single business domain (deliveries, complaints,
inventory). Functions accept a pandas DataFrame and return a
structured summary that downstream layers (AI reporting, dashboards)
can consume.
"""
